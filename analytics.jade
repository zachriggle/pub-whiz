!!! 5
html
  head
    include js/ga.js
    script
      function doNotTrack() {
      _gaq.push(['_setCustomVar',
        1,                // This custom var is set to slot #1.  Required parameter.
        'no_report',      // The name acts as a kind of category for the user activity.  Required parameter.
        'true',           // This value of the custom variable.  Required parameter.
        1                 // Sets the scope to session-level.  Optional parameter.
      ]);
      }

  body(body onLoad="javascript:doNotTrack();")